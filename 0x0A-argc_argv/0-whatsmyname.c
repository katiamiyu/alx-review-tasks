#include <stdio.h>
/**
* main - entry point of app
* @argc: argument count
* @argv: arguement vectors
* Return: always (0) success
*/
int main(int argc, char **argv)
{
	printf("%s\n", argv[0]);
	return (0);
}
